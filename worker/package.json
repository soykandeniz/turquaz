{
  "name": "turquaz-content-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply turquaz-content --local",
    "db:migrate:remote": "wrangler d1 migrations apply turquaz-content --remote",
    "check": "node --check src/index.js"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  },
  "dependencies": {
    "sanitize-html": "^2.17.7"
  }
}
